# challenge name(s); defaults to all compatible challenges
challenge:
- aido-LF*state*

# conventions + format
protocol: aido5-db20-state-z2
# any user label - displayed in "challenges list"
user-label: "minimal-agent-state"
# any JSON payload
user-payload: {}
